---
name: deploy-release
description: Execute a production release for Path of Dust per REFACTOR_PLAN.md section 13. Use for every merge-and-deploy.
---
Sequence, no steps skipped, per-item verdict in the report: (1) merge (conflicts: trivial keep-both only; anything substantive in combat.rs or the write path is STOP-and-report); (2) full suite green, --quiet, counts only; corpus regeneration only here, with diffs attributed; (3) determine bot need — `git diff --name-only <old-deployed>..<new>` against the bot's dependency set (`bot/**` + root `Cargo.lock` — since 2026-09-08 the root manifest is virtual, the bot is the `bot` member, and it has deliberately no dependency on `game`, so `game/**` changes never move the bot; REFACTOR_PLAN §13A "Conditional bot redeploy" is authoritative. There is no bot on the Linux game box: a §13B game release never moves it): a clean diff against that set means "diff-clean" — skip the bot entirely (no stop, no watchdog toggle; report `bot: unchanged, not redeployed (diff-clean)`); a freshly-built bot binary hashing differently doesn't override a clean diff (Rust release builds aren't reproducible) — note it, still skip; any uncertainty in the diff read → deploy both, as before. Game always deploys unconditionally on any behavior change. Disable watchdogs for whatever's moving; stop bot (only if deploying) then game; (4) SHA-256 old/new for whichever binaries move, back up to a per-release backup dir (bot entry only when it deploys) with the pinned fight-summary snapshot; (5) swap; game up + health-check before bot starts (if deploying); re-enable watchdogs for whatever moved; (6) patch-notes entry; (7) push master; (8) report: per-item hash or BLOCKED+reason, plus the bot's deploy/skip determination and which diff paths drove it. Rollback = restore the backup dir binaries. Backup dirs older than 7 days are prunable.
