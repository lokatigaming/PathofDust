---
name: verify-order
description: Execute a log-parser verification order against live Path of Dust fights. Use for every verification task.
---
Discipline: (1) STEP 0 — confirm the named commits are in the running binary (hash prefixes) before judging anything; build-check any character via their Memories state before cross-fight claims. (2) Close items only with arithmetic — expected vs observed, shown. (3) Two clocks: events[].atMs is display-compressed; rolls[].atMs is real. Reconcile before calling a cadence anomaly. (4) Ledger: numbered items, canonical numbering; OPEN/CLOSED with the evidence that moved it; carry-forwards listed. (5) Distinguish "verified absent" from "never exercised" — untriggered guards stay open, not closed. (6) Report raw log lines for any failure; never adjust data to fit a claim.
