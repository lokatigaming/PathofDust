# Wiki Impact Log

Player-facing changes that may require wiki updates. The wiki session consumes
this. Append one line per change; newest at the bottom.

Format: `<file>:<const or fn> — what changed — affects <bosses|crafting|passives|commands>`

---

src/passive_tree.rs:MONK_NODES — Hundred Fists and One Hundred Hands swapped tiers: "One Hundred Hands" is renamed **"Flow like Water"** and is now the Specialization under Flowing Strikes (max rank 4), while Hundred Fists is now a Modifier under Pressure Point (max rank 3). Chakra of Many/Light/Life re-parented from Hundred Fists to Flow like Water. Both nodes' effects and description text are unchanged — only names, tiers, and parents moved — affects passives
src/adventure/combat.rs:onehundredhands_bonus_stacks — clamped to rank 3, so Flow like Water's 4th point only unlocks its Modifiers and does not grant a 4th bonus stack (matches every other Specialization and the node's own "up to 3 at 3/3" text) — affects passives
src/passive_tree.rs:"doom" node — detonation cut from 3/6/9% to 2/4/6% of damage dealt to the cursed target; the node's own description prose was updated to match, so the rendered passive text follows automatically — affects passives
src/adventure/combat.rs:ELEMENTAL_PROC_CHANCE_DIVISOR — 50.0 → 10.0, a 5x increase to how often Fire/Cold/Chaos/Lightning/Divine ailments apply on a landed hit; per-stack strength, stack caps and duration all unchanged — affects passives
src/adventure/combat.rs:apply_late_stage_penalty (new fn) — the late-stage damage penalty vs a real boss now applies to every damage source, not just normal attacks; previously bypassed by Lingering Effect DoT ticks, Warlock's Doom detonation (both on-expiry and on-death variants plus their Apocalypse splash), Mage's Volatile Magic splash, Slayer's Hemorrhage/Wound explosion, and reflected damage. Slayer's Culling Strike execute is deliberately exempt (guaranteed kill, no damage amount to scale) — affects passives
src/adventure/item.rs:Item::reforge_crit_used/recombine_crit_used — Reforge's and Recombine's once-per-lineage crit gates are no longer permanent sticky flags; they're now derived from whether the crit-granted bonus affix is still actually present on the item (`Item::crit_bonus_affixes`, now source-tagged). Removing that affix by ANY means (Annulment, a future crafting method, etc.) re-opens the gate. Old saves migrate via `migrations::migrate_crit_flag_to_affix_tracking` — affects crafting
src/adventure/manager.rs:AdventureManager::new (one-time equipped-item backfill) — every currently-equipped item whose old crit-used flag was true but had no surviving crit-granted affix gets a fresh random bonus affix granted immediately, tagged as a Reforge crit affix — affects crafting
Pure refactor, no behavior change — hoisted these bare literals into named pub(crate) constants so the wiki's constant audit can wire placeholders to them: src/adventure/combat.rs:DRAGON_SLOW_MULT (1.5), src/adventure/combat.rs:FIRE_DEMON_HEAL_MULT (0.5), src/adventure/combat.rs:CTHULHU_DEBUFF_CAP (0.9, Cthulhu's own two read sites only — not the separate Thornedhide/Soul Stone/Bloodpact Triage 0.9 caps), src/adventure/combat.rs:LICH_SUMMON_CADENCE_MS (2000ms), src/adventure/combat.rs:LICH_ADDS_PER_SUMMON (5), src/adventure/combat.rs:LICH_MAX_ADDS (20, hoisted out of simulate_battle's function body to module level), src/adventure/character.rs:RECOMBINE_CRIT_CHANCE (0.05), src/adventure/manager.rs:PANEL_REFORGE_DUST_PER_TIER (30), src/adventure/manager.rs:POLISH_PERFECT_SAND_COST (12), src/adventure/manager.rs:POLISH_SAND_COST_PER_QUALITY_PCT (10.0) — affects bosses|crafting
src/commands.rs:"pinfight" (new mod command) — !pinfight copies the most recent coarse-tier and detail-tier fight-log files into a pinned/ folder that the normal rolling-window pruning never touches, announcing the pinned fight's id in chat; also visible on the /admin/tunables dashboard. Preserves bug-report evidence past the ~5-minute detail-tier window without raising the retention caps for everyone — affects commands
src/passive_tree.rs:"echoingpower" node — tooltip-vs-data fix, not a balance change: description said Spell Echo's re-cast damage reaches 80% at 3/3, but the stored effect (0.15/0.15) has always computed 95% in combat — text corrected to match the real, unchanged data — affects passives
src/adventure/character.rs:Character::combat_intervene — real bugfix (wiki audit finding #2): combined gear+tree Intervene could reach 75% (two 50%-capped sources combining multiplicatively) despite the documented 50% per-character cap; a post-combine .min(0.5) now enforces it. Lowers effective Intervene for anyone stacking both gear and "oath" investment near their individual caps — affects combat
src/adventure/combat.rs:drain_leech_window (new fn) — real bugfix (wiki audit finding #3): the life-leech per-second cap (LIFE_LEECH_CAP_PER_SEC) used a reset-based window that let a leech build burst to ~2x the intended cap by timing hits across the 1000ms reset boundary; now a genuine leaky-bucket rolling window, draining continuously instead of resetting in a lump sum. Lowers peak leech-healing burst for Slayer leech builds; steady-state leech over time is unchanged — affects combat
Pure refactor, no behavior change — fulfills the wiki session's constant-hoist request: src/adventure/combat.rs:DEFENSIVE_STAT_HARD_CAP (0.95, the combined-Evasion and combined-Block+DR ceiling, 3 call sites), src/adventure/combat.rs:LATE_STAGE_PENALTY_STAGE_OFFSET (2000.0), src/adventure/character.rs:MELEE_BASE_ATTACK_INTERVAL_MS/RANGED_BASE_ATTACK_INTERVAL_MS/HEAL_BASE_ATTACK_INTERVAL_MS (1400/900/1700 ms), src/adventure/manager.rs:BOSS_ATTACK_INTERVAL_BASE_MS/BOSS_ATTACK_INTERVAL_PARTY_SCALING/BOSS_ATTACK_INTERVAL_FREQUENCY_DIVISOR (1100.0/0.15/2.0), src/adventure/character.rs:BASE_CRIT_CHANCE/BASE_CRIT_MULTIPLIER (0.05/2.0), src/adventure/character.rs:STARTING_FREE_MODEL_CHANGES/STARTING_FREE_RECOMBINES/STARTING_FREE_ARCHETYPE_CHANGES/STARTING_FREE_PASSIVE_RESPECS (1/1/2/1 — also replaced the generic default_one_u32/default_two_u32 serde helpers with field-specific ones reading these same constants, so Character::new and the old-save migration default can never drift apart from each other), src/adventure/item.rs:INDESTRUCTIBLE_ITEM_CHANCE/ITEM_DURABILITY_MIN_USES/ITEM_DURABILITY_MAX_USES (0.01/7/13), src/adventure/affix.rs:AFFIX_COUNT_4_CUMULATIVE_THRESHOLD/AFFIX_COUNT_3_CUMULATIVE_THRESHOLD/AFFIX_COUNT_2_CUMULATIVE_THRESHOLD (0.01/0.06/0.16 cumulative), src/adventure/combat.rs:SHIELD_CAP_MULT (4.0) — affects combat|passives|items|character progression
src/adventure/manager.rs:full_player_fight_stats — hits/crits/evaded now exclude DoT ticks, new dot_ticks/dot_damage fields; damage totals unchanged — affects commands
src/adventure/combat.rs:simulate_battle (boss_pierce_pct) — new mechanic, not a bugfix: a stage-scaled fraction of every REAL boss's attack (named world bosses only — never basic encounters, never player attacks, never a boss's own DoT/true-damage abilities like Doom or Hemorrhage explosions) now resolves as unavoidable, unmitigable true damage before the rest of the hit runs the normal evasion/block/damage-reduction/crit pipeline as usual. Formula: boss_pierce_pct = PIERCE_CAP_PCT * stage^2 / (stage^2 + PIERCE_H^2) — a quadratic-then-asymptotic ramp that reaches half of PIERCE_CAP_PCT at stage PIERCE_H and climbs toward, but never reaches, PIERCE_CAP_PCT itself. At live stage ~821 this is ≈7% pierce; ~18% at stage 1500; ~35% at stage 3000. Both PIERCE_CAP_PCT (default 0.5 → 50%) and PIERCE_H (default 2000) are LiveTunables (adventure-live-tunables.toml, admin-editable at /admin/tunables, re-read every fight) — NOT compile-time constants, so the wiki's placeholder substitution can't read them the usual `crate::adventure::CONST` way; wiring these two placeholders needs a live-value read path instead (e.g. through AdventureManager::live_tunables()), flagging this explicitly since every other wired placeholder to date has been a compile-time constant. Shields still absorb the pierce portion and death-prevention passives (Guardian Spirit, Undying Fury, Verdant Burst, Soul Stone, Chakra of Life) still trigger on it normally — only evasion/block/damage-reduction/intervene are bypassed. Directly increases effective incoming boss damage for every build, scaling up with stage — healers and shield-granting passives are more valuable now than before this landed — affects combat
src/adventure/combat.rs:resolve_hit (boss pierce split-point bugfix, same day as the above) — the formula/percentages above are unchanged, but the initial implementation carved the pierce fraction off the boss's raw pre-roll base damage instead of the fully-rolled hit, so pierce silently ignored boss_power_mult/increased-damage/crit almost entirely at live stage scaling (the pierce chunk was a rounding error, the normal mitigable pipeline was still carrying nearly all the real damage — the mechanic was live but had negligible real effect). Fixed same-day, before any real fight data was captured against it: pierce now splits AFTER the attacker-side roll (crit, increased damage, boss_power_mult — baked into the boss's base attack stat — and the late-stage penalty), so a boss with real crit/increased-damage investment now has its pierce chunk scale with that too. Invariant: against a target with zero evasion/block/DR, total damage taken is byte-identical whether pierce is on or off — pierce only ever redistributes WHERE damage is unavoidable, never how large the rolled hit is. No formula/percentage change, no wiki text change needed — affects combat
game/src/adventure/published_constants.rs (new) + wiki.rs:wiki_placeholder_map — architecture refactor: BUILTIN_COOLDOWN_S/BUGREPORT_COOLDOWN_S/VOTESKIP_COOLDOWN_S/MIN_VOTE_VOLUME/MAX_VOTE_VOLUME's data source changed. These 5 placeholders used to read the bot's own constants directly at render time; wiki.rs now lives in the `game` crate (owner's ruling: the standalone game must serve its full web UI, wiki included, with no bot process running), so it can no longer reach bot-side code directly. The bot now writes its own values for these 5 (all compile-time-fixed on the bot side, never live-changing) to a small shared file (`bot-published-constants.json`) once at every startup; wiki.rs reads that file per-request. Same real numbers in the normal case (bot has started at least once since deploy). New fallback state, previously impossible: if that file is missing or unparseable (bot never started, or a standalone `game` instance with no bot at all), all 5 placeholders render the literal word "varies" instead of a number. Confirmed live via a real standalone smoke test (game binary alone, no bot, /wiki/commands page): renders correctly, no crash — but the surrounding markdown text wasn't written expecting a word there, so it currently reads awkwardly ("Shared variess" from a template appending a literal "s" after the value; "!vv <varies-varies>" for the vote-volume range) — cosmetic only, worth a wording pass in wiki/commands.md whenever convenient (that file is owner/wiki-session-edited only, not touched here) — affects commands|wiki infrastructure

NOTE — everything below is `feature/elementalist`, NOT yet merged to master/deployed. A brand-new 12th playable class (Elementalist), built in 6 owner-approved stages; full spec at docs/elementalist_spec.md, live stage-by-stage status/decisions at ELEMENTALIST_PROGRESS.md (both repo root). Flagging here now (rather than waiting for merge) so the wiki session has the full picture whenever this branch lands — every entry below describes real, tested, but not-yet-live mechanics.
src/passive_tree.rs:ELEMENTALIST_NODES (new) + src/adventure/character.rs:Archetype::Elementalist (new) — adds the Elementalist archetype: Ranged, base class effect is splash scaling with level (same shape/magnitude as Ranger's own splash), no baseline heal_power_pct. Full 39-node passive tree (3 skills → 9 specializations → 27 modifiers) — see docs/elementalist_spec.md for every node's name/description/numbers — affects passives
src/adventure/combat.rs:elementalist_elemental_damage_pct (new fn) + Shocking/Chilling/Scorching Focus/Overshock/Polar Flux/Incinerate — Elemental Focus branch: flat elemental-damage-pct gain (this codebase's existing Fire/Cold/Lightning proc-chance stat, not a new damage type), gear-scaled per-element modifiers, and a "apply debuffs more frequently" mechanic via a second independent proc roll (Monk's Chakra of Light pattern, generalized to all 3 elements). Electrical Overload/Blizzard are plain crit-damage/crit-chance grants. Conflagration is its own independent multiplicative damage layer (not folded into the shared increased-damage pool) — affects passives
src/adventure/combat.rs:tick_righteous_fire (new fn) — Righteous Fire branch (the class's whole "part 1 + part 2", 13 nodes): a new once-a-second tick drives self-burn + enemy damage (true damage, splash-based target count), Healing Flames' self-regen (irregular 3%/6%/10% per rank) + Fanning Flames' shared portion + Shielding Flames' bonus shield, Relentless Flames' stacking enemy vulnerability, Cauterizing Flames' reduced-healing debuff, and Ashes to Ashes' unconditional execute (any enemy, bosses included, below a % of the Elementalist's own max HP — no boss exemption, per the owner's explicit spec). Cleansing Flames gets its own separate 4-second tick (src/adventure/combat.rs:tick_cleansing_flames) — a probabilistic debuff cleanse plus an unconditional Enshrouded/Guardian/Shielding Fire buff refresh on nearby allies — affects passives
src/adventure/combat.rs:NextEvent::Revive + try_schedule_rising_phoenix_revival (new) — Rising Phoenix: up to 1-3 nearby allies (per rank) who'd survived ≥3s revive 1s after death, at 25% max HP (not spec'd — a judgment call, easy to retune). Required a real fix to src/adventure/manager.rs:build_player_vitals (PlayerVitals::died_at_ms now tracks the FINAL death in a fight, not the first — a no-op for every existing fight, since none could have more than one death before this) — affects passives|character progression
src/adventure/combat.rs:spawn_golem/GOLEM_STAT_SCALE (new) + src/adventure/character.rs:GolemType/Character::golem_slot_types (new) — Golem Master foundation: 1-3 summonable golems (33% of the Elementalist's own combat stats — max HP, attack, crit chance/damage, evasion, DR, block — attack cadence copied as-is), Basic type only so far (Thunder/Flame/Water land in a later stage). Elementalist's own damage drops 33% per golem, additive (99% reduction / 1% of normal damage at 3 golems), as its own independent multiplicative term. Golems die instantly when their summoner dies and never count toward the fight's own alive-party-check (owner-mandated rule — otherwise an all-players-dead fight with a golem still up would never end). New `/passives/set-golem-type` picker (one dropdown per unlocked slot) — affects passives|commands
src/adventure/combat.rs:thunder_golem_redirect (new) — Thunder Golem's own base behavior, completing the Golem Master branch: absorbs all externally-sourced damage the party would take (a real redirect of WHO gets hit, applied once at the top of `apply_hit` — the single choke point every enemy-hits-party site in this file funnels through, verified against a real seeded fight simulation per BossKind, not just code review), cannot be shielded or healed by any means, reforms 4/3/2s after dying at full hp. Gigantify raises its own hp-contribution (33% → up to 132%); Growing permanently grows its max hp a bit more on every reform within a fight; Terrifying explodes for a fraction of its own hp against every alive enemy when it dies. Flame Golem inherits a fraction of the caster's own fire-damage-debuff-chance stat (Volcanic Ash), attacks faster (Blazing — an irregular 6/9/18% per rank), and deals more damage (Surging). Water Golem converts all its own damage into party healing at a 100/200/300% rate (faster heal-share casts at higher ranks, not bigger individual heals — same "excess healing power speeds up casts instead" mechanic every healer archetype already has), Singing grants the whole party more effect from heals/shields received (a fight-start snapshot, not live-tracked against the Golem's own alive status), and Shattering sends damaging icicles to nearby enemies when any enemy dies in its presence. This is the final stage of the Elementalist build — every passive tree node this class has is now real — affects passives

src/adventure/manager.rs:announce_encounter_result (now calls record_fight_for_batch) + src/adventure/announcements.rs:aggregate_batch/format_batch_summary (new) — routine fight-result chat announcements (the per-fight "party of N heroes..." Top DPS/Tanks/Heals line, Basic and Boss encounters alike) no longer post per fight; they accumulate into one batched summary every LiveTunables::fight_summary_batch_size fights (new tunable, default 10, admin-editable at /admin/tunables, re-read every fight) or after 5 minutes with no new fight, whichever comes first. New message shape: "Last N fight(s): WW-LL[ · stage X → Y[ (peak Z)]] | 🗡️ Top DPS: ... · 🛡️ Top Tanks: ... · 💚 Top Heals: ..." — top-3-per-category totals summed across the whole batch, not per-fight, ranked fresh; degrades to 2 then 1 named leader per category, then drops the MVP breakdown entirely (record line only) if the message would still exceed the ~450-char target. Per-fight loot/gear-crit lines and the separate pre-fight boss-intro hype line (!event intro) are unaffected and still post immediately — affects commands

src/adventure/combat.rs:reform_thunder_golem (Growing bugfix) — Thunder Golem's Growing modifier now stacks ADDITIVELY off the golem's original spawn-time max hp on every reform (`max_hp = reform_base * (1 + rank_pct * reform_count)`), not compounding onto its own already-grown value as before. At rank 3 (100%/reform), a golem that's reformed 12 times in one fight now sits at 13x its original hp, not the runaway 4096x (2^12) the old compounding formula produced in a long fight. Terrifying's own death explosion (reads the golem's current max hp) automatically scales with the corrected, slower curve too — affects passives
src/adventure/combat.rs:is_protected_golem/is_damage_immune (new) + thunder_golem_redirect (extended) — Basic/Flame/Water golems are now UNCONDITIONALLY immune to damage and never selected as an enemy attack's target, closing a real gap where their immunity previously only held as a side effect of Thunder Golem's own redirect: a non-Thunder golem picked directly as a target (or hit during a Thunder Golem's reform gap, when none is alive to absorb) previously took full damage like a real player. Fixed with two layers — every target-selection pool (primary attacks, splash, Volatile-Magic-style true-damage AoE) now excludes non-Thunder golems outright, and every direct damage-application site that doesn't route through the redirect (DoT ticks, Doom detonation, reflect, true damage) now runs a blanket immunity check. Known, accepted consequence: during a Thunder Golem's reform gap, attacks that used to leak onto a non-Thunder golem now land on a real player instead — that gap is Thunder Golem's own intended weakness, this just stops it bleeding onto the wrong unit type — affects passives
src/adventure/combat.rs:elementalist_per_level_elemental_pct (new) — Elemental Focus and Scorching Flames now actually multiply by the Elementalist's own character level (the spec has always said "per level" for both, the code never did) — rank 3 Elemental Focus (15%) applies in full to fire, cold, AND lightning independently at level×15%; rank 3 Scorching Flames (30%) stacks on top for fire only. Balance note: these stats are elemental-PROC-CHANCE inputs (clamped to 100% past a raw value of 10.0), not damage multipliers, so this does not inflate personal damage-per-hit for anyone near max level — full detail in docs/elementalist_spec.md. Base-class splash (the third "per level" mention in the spec) was separately verified already correct, unchanged — affects passives
src/adventure/manager.rs:full_player_fight_stats/first_player_to_die (golem attribution) — a golem's own damage dealt, damage absorbed/taken, and healing done (including a Thunder Golem's party-tanking absorption and a Water Golem's Replenishing heals) now roll up into its owning Elementalist's stats everywhere a per-player total is shown or ranked: Top DPS/Tanks/Heals in fight announcements (per-fight and the batched summary), and the /fights dashboard. A golem never appears as its own named leaderboard entry again — previously it did (any non-boss unit got its own row), including in the one-time Celestial Shard "top healer" and Unique Shard launch-giveaway awards, which would have silently tried to grant to a golem's own (non-existent) character id had one ever topped a category. Also fixed a real, already-live bug found during this audit: the /fights dashboard's "First Down" field could report a Thunder Golem's own routine reform-cycle death as if the owning Elementalist had gone down — golem deaths are now excluded entirely, never attributed to anyone. Dust/sand/loot grants were checked and confirmed unaffected — they're flat, per-participating-player rewards with no connection to per-unit damage or healing — affects passives|commands

---

## Wiki catch-up pass (2026-08-19) — processed

Entries 10-25 (Monk rename, Cthulhu/Dragon/leech/intervene fixes, constant
hoists, pinfight, Echoing Power, boss pierce, bot→game published constants)
were already handled by the prior wiki completeness pass (commits e282330
through d01be34, plus the placeholder-map hoist-fulfillment follow-up) —
re-checked against current wiki text this pass, still accurate, nothing to
change.

This pass, oldest-unprocessed-first:
- Entry 26 (bot-published-constants "varies" wording): fixed the "Shared
  variess" / "!vv <varies-varies>" awkward phrasing in wiki/commands.md
  (now reads correctly whether the value is a real number or "varies").
- Entries 28-34, 40 (Elementalist class + all 3 branches, Golem
  foundation + all 4 types, the Elemental Focus/Scorching Flames
  per-level fix): documented in full against docs/elementalist_spec.md
  as source of truth — new "The Elementalist" section on wiki/classes.md,
  new wiki/golems.md for the whole golem system. The 39-node tree's own
  real numbers need no hand-transcription — they're already live on the
  code-generated /wiki/passives page now that ALL_ARCHETYPES includes
  Elementalist.
- Entry 36 (fight-announcement batching): documented in
  wiki/getting-started.md's new "Chat Announcements" section.
- Entry 41 (golem attribution): documented in wiki/golems.md's "Rules
  Worth Knowing" section.
- Entries 24-25 (boss pierce) and 38-39 (Growing/non-Thunder-immunity
  bugfixes): checked against every currently-published wiki page —
  none of these change any existing wiki claim (boss pierce isn't
  referenced anywhere in wiki/combat.md at all yet; Growing/immunity are
  internal-mechanics-only, no player-facing number was ever published
  for either). Boss pierce specifically is a real combat-page gap, not a
  contradiction — flagged in this pass's report for a future pass, out
  of this pass's explicit Elementalist/Golem/announcement/attribution
  scope.

Tagged "balance in flux" per the owner's explicit instruction (mechanic
documented, numbers deliberately left loose, both relayed directly by the
owner rather than found in this log): golem stat inheritance being
corrected from base stats to fully-buffed effective stats, and Thunder
Golem's incoming absorbed-damage-redistribution-on-death mechanic. Both
called out on wiki/golems.md.

Not documented, per instruction (not yet deployed): Memories (build
loadouts), Divine Dust (sacred affix currency) — not found anywhere in
this log, docs/elementalist_spec.md, or ELEMENTALIST_PROGRESS.md either,
consistent with "still in development." Nothing accidentally published.

No contradiction found between this log's entries and
docs/elementalist_spec.md's current numbers — the spec doc's own inline
"(2026-08-19: ...)" addenda already incorporate every balance-ruling
correction referenced in this log (Growing's additive-not-compounding
fix, the per-level elemental-pct fix), and every number checked against
it during this pass matched exactly.

---

src/adventure/combat.rs:reform_thunder_golem/apply_rising_phoenix_revival (revival-healing attribution) — a real, live leaderboard-corrupting bug, confirmed via fight-log audit: a Thunder Golem's reform (full-hp restoration after death) was logging as a `Heal` event, and once golem attribution shipped, rolled that phantom "healing" into the owning Elementalist's own healing_done stat — one audited fight showed 22 reforms contributing 301.7M of a reported 316.5M total "healing" (95.3% phantom). Fixed: a reform is now a pure unit-lifecycle event (its existing `SkillCast` marker is the only log trace), never a `Heal` — credits nothing, rolls up nothing. Water Golem's Replenishing is unaffected, still real healing via the same shared heal pipeline every other healer uses. Rising Phoenix's revive HP now correctly credits the CASTING Elementalist's own healing_done (previously credited the revived ally's own id, a self-heal misattribution) and routes through the real heal pipeline so modifiers like Water Golem's Singing apply normally, logged with a new `isRevive` marker so audits can tell a revival apart from a routine heal cast — affects passives
src/adventure/combat.rs:spawn_golem (golem stat inheritance) — golems' per-hit damage output was measured at ~3% of the owning Elementalist's own output instead of the intended 33%, an 11x gap: increased_damage and conflagration_dmg_pct (gear/tree damage multipliers) were never inherited from the summoner at all, only base stats (max hp/atk/crit/evasion/DR/block) were. Fixed by inheriting those two multipliers at FULL value (not scaled to 33% like the base stats — the math only lands near the intended 33% per-hit RATIO if a multiplier passes through whole, since `(atk×0.33)×(1+full_multiplier) = 0.33×owner_damage` exactly, while scaling the multiplier down too compounds against the already-scaled base stat instead of canceling out). Real, meaningful damage buff for any Elementalist running gear/tree damage investment with golems out — see docs/elementalist_spec.md's balance projection for the expected combined (self+golem) output band — affects passives
src/twitch/chat.rs:ChatClient::say (observability) — every bot-originated chat message (commands, redemptions, and every game announcement) is now logged in full to bot.log before sending, regardless of outcome — previously only a truncation warning or a send failure was logged, so a fight-log audit had no way to verify what actually appeared in chat. Log-only, no behavior change — the existing 500-char truncation guard in this same function (confirmed already in place, live-tested 2026-08-13) is unrelated and untouched — affects commands

---

## Wiki update pass (consolidated-fix release) — processed

Entries 101-102 (this log's two new entries since the last wiki pass),
checked against docs/elementalist_spec.md's updated Golem Master section
and its new "Balance projection" addendum as source of truth:

- Entry 101 (reform/Rising-Phoenix healing-attribution fix): documented
  in wiki/golems.md's "Rules Worth Knowing" - a golem reform grants no
  healing credit to anyone (pure lifecycle event); Water Golem
  Replenishing still credits the owner normally; Rising Phoenix's
  revival heal now correctly credits the casting Elementalist, not the
  revived ally (also noted on wiki/classes.md where Rising Phoenix is
  named, since it's a Righteous Fire node, not a golem one).
- Entry 102 (golem stat inheritance): the "balance in flux" tag from
  the prior pass is now resolved and finalized on wiki/golems.md, per
  the spec's own explicit "FULLY-BUFFED EFFECTIVE stats" wording -
  documented with the real nuance the spec calls out (damage
  multipliers inherit at FULL value, not scaled to 33% like everything
  else - that's the actual fix, not just a base-vs-effective swap), plus
  the spec's new balance projection (combined self+golem output should
  land near 100% of solo at any golem count, crit-heavy builds trending
  a bit under).

Entry 103 (chat-send logging) - internal observability only, no
player-facing behavior, nothing to document.

Still tagged "balance in flux" (not yet resolved, per direct owner
instruction rather than a new log entry - not found written down in
this log or the spec doc yet):
- Thunder Golem's absorbed-damage redistribution-on-death mechanic -
  built, awaiting deploy. Flux tag kept as-is on wiki/golems.md.
- Ashes to Ashes - a full rework is queued (replacing current behavior,
  not just retuning it). New flux note added to wiki/classes.md - the
  page never stated the live 100/200/300% figures to begin with, but
  the note now makes clear the MECHANIC itself (not just its numbers)
  shouldn't be treated as final.

Still not documented, per instruction (not yet deployed, still not
found in this log or either spec/progress doc): Memories, Divine Dust.

---

NOTE — everything below is Release 1 (branch `feature/thunder-redistribution-golem-integrity`), just merged to master and deployed live (2026-08-19) — the "flux tag" on Thunder Golem's absorbed-damage-redistribution mechanic noted in the wiki pass above is now resolved; real numbers below. Full detail at docs/elementalist_spec.md's "Release 1, Part A/B" sections.
game/src/adventure/combat.rs:golem_may_produce_heals/is_heal_immune + the wound-leech gate in resolve_hit (golem integrity fixes A1/A2/A4) — three real gaps found in a verification pass over the shipped golem work: (1) golems could self-heal off Festering Wound's leech (a target-side field, independent of the attacker's own leech stat, which golems never had anyway) — now gated on `!attacker.is_golem`; (2) Thunder Golem's heal/shield immunity only lived inside apply_heal, not the several hand-rolled heal-write sites that bypass it (most notably the Guardian Spirit/Undying Fury/Verdant Burst/Soul Stone/Chakra of Life death-prevention cascade) — now a shared immunity check applied at every direct HP-restoration site in the file; (3) the Lingering Effect heal-flavor tick pushed a zero-amount Heal event on every 50ms tick even when the target was already at full hp — now suppressed, same convention every other heal site already used. Fix (3) is NOT golem-specific — it cuts heal-event volume for every archetype's Lingering Effect ticks, not just Elementalist's — affects passives|commands
game/src/adventure/combat.rs:handle_golem_death/apply_thunder_redistribution_tick (new) + AttackSourceKind::Environmental (new) — Thunder Golem absorbed-damage redistribution: when an incarnation dies, a live-tunable fraction (LiveTunables::thunder_redistribution_pct, default 50%) of the total damage that incarnation actually absorbed splits evenly across every currently-alive real party member (never another golem) as an unmitigated 2-tick DoT spread across LiveTunables::thunder_redistribution_window_secs (default 2.0s), both admin-editable at /admin/tunables under a new "Elementalist" section. A lethal tick resolves like any other death (Rising Phoenix eligible). Tagged with a new, deliberately unattributed AttackSourceKind::Environmental source (sentinel attacker id, credits nobody's own damage_dealt) plus a distinct "Thunder Golem Redistribution" log marker per death. The owner's own tank-credit stat (full_player_fight_stats' golem-rollup) now nets out whatever got redistributed away, so a died-mid-fight incarnation only credits `(1 - pct) × absorbed` instead of the full amount (avoiding double-counting the portion that now shows up on the recipients' own damage_taken instead) — affects passives|commands

NOTE — everything below is Release 1.1 (branch `feature/thunder-redistribution-golem-integrity`, same branch, follow-up commits), merged to master and deployed live (2026-08-19). A live verification pass on Release 1 found redistribution delivering ~18% of its intended load; full per-item detail at docs/elementalist_spec.md's "Release 1.1" section. The parser's own post-deploy verification pass confirmed the redistribution and retaliate-proc fixes below; redistribution delivery improved but remains short (tracked as a Release 1.2 follow-up), retaliate procs verified at ~40x their pre-fix rate (see the splash-hit eligibility entry below).
game/src/adventure/combat.rs:apply_hit (already-dead-target guard) + the golem-death sweep now also running once more right before the fight-ending break (new `sweep_golem_deaths`) — two real bugs in Thunder Golem redistribution's own trigger coverage. A boss ability whose splash hits every unit at once (Dragon's Breath, Cube's 5-target splash) could land multiple hits on the SAME already-dead Thunder Golem in one turn (apply_hit had no `!alive` guard, unlike every sibling damage site in the file) - fixed with one. And a golem that died in the SAME tick that ended the fight (a full party wipe, or dying alongside the boss's own final blow) never got redistribution scheduled at all, since the fight-ending break ran before the sweep that would have caught it - fixed by running that sweep once more, right before the break — affects passives
game/src/adventure/combat.rs:handle_golem_death (merge, not overwrite) — the actual root cause of the ~18% delivery rate: scheduling a new redistribution for a recipient who still had ticks pending from an EARLIER, not-yet-delivered death silently discarded the earlier amount (a straight overwrite) - easily reached with a Thunder Golem dying twice inside the ~2s window one death's own ticks are spread across (short reform delays at higher ranks). The owner's own tank-credit field had already been debited for the full theoretical amount regardless, so the discarded portion just vanished - never reaching the party, never staying with the owner either. Fixed by merging any still-owed amount into the new schedule instead — affects passives
game/src/adventure/combat.rs:apply_rising_phoenix_revival (SkillCast naming) — the SkillCast marker for a Rising Phoenix revival named the revived ally; the Heal event right next to it already correctly named the casting Elementalist. Now both agree — internal-observability-only, no player-facing number changed — affects passives
game/src/adventure/combat.rs:apply_hit (defender_may_react, replacing counts_as_primary_hit at two gate sites) — real bugfix, item 7: Warrior's Retaliation and the shared Rogue's Voidstep/Monk's Counterflow/Druid's Wild Fury counter-attack group were gated on a flag scoped to the ATTACKER's own chain-prevention concern, which incorrectly also excluded the DEFENDER's reactive procs from firing against a splash hit — roughly 95% of evaded/survived hits in a typical fight, since splash is the majority damage source. Fixed so these now trigger on ANY defensive roll (evade or survived landed hit), including against splash — parser-verified live at ~40x the pre-fix proc rate. Meaningfully increases realized uptime for anyone invested in these passives — affects passives|combat

NOTE — everything below is the Memories feature (branch `feature/memories`), merged to master and deployed live (2026-08-19). Full design at docs/memories_spec.md; execution log at MEMORIES_PROGRESS.md.
game/src/adventure/memory.rs (new) + Character::memories/memory_slots + AdventureManager::save_memory/load_memory/rename_memory/delete_memory — MEMORIES, a new player-facing feature: each character gets 3 saved passive-build slots (a per-character value defaulting to 3, not a fixed constant — a future feature can grant more). A Memory captures the whole build (archetype, primary tree, Split Personality secondary archetype + its tree, Elementalist golem slot types); loading one fully becomes that build. Loading is FREE and bypasses both ARCHETYPE_CHANGE_COST and PASSIVE_RESPEC_COST once a build is saved — so one paid class change plus a saved Memory buys unlimited free switching between those builds thereafter (an accepted, deliberate economy trade-off, recorded in docs/memories_spec.md). Loading is allowed OUT OF COMBAT ONLY and is rejected during a fight; no queuing, no mid-fight swaps. Points earned since a snapshot was saved are left unspent rather than auto-spent. Stale allocations (node removed, or its rank cap lowered) are dropped, refunded as unspent, and reported — a load never fails outright. Managed entirely on the /passives page (save/load/rename/delete); NO new chat commands this pass. Memory names are player-authored, capped at 150 characters, and run through a new content blocklist — affects passives|character progression

---

NOTE — patch-notes.json catch-up pass (2026-08-19). Player-facing patch-notes.json (repo root, gitignored, NOT tracked by git — separate from this log and from wiki/) had gone ~1 day without an entry. Backfilled a full "August 19, 2026" top-level entry covering everything deployed since the "August 18, 2026" entry, verified against git log rather than the requester's own recollection: the Elementalist class launch (the single biggest item in the window — notably absent from the initial request's own content list), Memories, golem damage/attribution fixes (the ~3%-vs-33%, 11x-gap fix; reform≠healing; owner-attribution on damage/healing/leaderboards; Rising Phoenix revival-healing credit), Thunder Golem's absorbed-damage redistribution (WORDED CAREFULLY as damage dealt to the party on golem death, NOT healing — see the raw description two entries up: it's an unmitigated DoT split across survivors, half of what was absorbed, the other half forgiven; explicitly marked in the patch note as still short of its intended 50% share pending a Release 1.2 follow-up, per the resolution above), the Growing/non-Thunder-immunity golem-integrity fixes, the Retaliation/Voidstep/Counterflow/Wild Fury splash-hit fix (~40x procs), fight-announcement batching, scroll-position-restore QoL, and the x5/x10/x50 repeat-craft reliability fix (this turned out to be genuinely player-facing — a real bug where repeat-craft could silently stop partway through — despite an earlier expectation that it was internal-only). Excluded per instruction as internal-only: the observability/logging release, the admin live-tunables page (already self-documented as no-op in the entry the deploy session added ahead of this pass), and the item-nickname/template security fix.

**patch-notes.json schema/process note for future sessions (including the deploy session, which now appends to this file directly as part of its own procedure):** top-level array of `{date, sections}`, newest date first; `sections` is `[{heading, items: [...]}]` with optional `image`/`iframe` string fields on a section for embedded charts (see the Aug 18 "Crit Stacking" entry for the iframe pattern). Player-facing tone throughout — what changed for the player, not internal plumbing (backend-only changes that must be mentioned use a "Behind the Scenes:" heading and say so plainly, per existing convention). **When today's date already has an entry (as it did going into this pass — the deploy session's own live-tunables note), APPEND new section objects into that entry's existing `sections` array — do not create a second same-date top-level entry, and do not rewrite/reorder sections already present.** The file is plain JSON with no schema validation on write — sanity-check it still parses after any edit.
game/src/adventure_web.rs:render_passive_tree_page (copy only, no formula change) — the /passives page said "1 point from the start, +1 every 5 levels" in two places; passive_tree.rs:points_for_level has actually been `1 + level / 4` since 2026-08-16 and the copy never followed. Both now say "every 4 levels". NO player-facing number changed — the page was simply describing the formula wrongly, so any wiki text copied from it may be wrong too and is worth checking — affects passives

NOTE — everything below is Stage 1 of live-tunable passive values (branch `feature/live-tunables`), merged to master and deployed live (2026-08-19). Design at docs/passive_tunables_spec.md; execution log at LIVE_TUNABLES_PROGRESS.md.
game/src/adventure/passive_overrides.rs (new) + PassiveNode::magnitude_at_rank (hook) + /admin/passives — PASSIVE VALUES ARE NOW LIVE-TUNABLE. An admin-only page can change the numeric value of any passive node at any rank, applied immediately with no rebuild and no restart (same idea as /admin/tunables, generalized to the tree). VALUES ONLY — node structure (which nodes exist, their max ranks, parents and unlock gating) is unchanged and not editable. With no override file present every node reads exactly its shipped value, so nothing changes until the owner tunes something. IMPORTANT FOR THE WIKI: 351 of 471 nodes are tunable as of Stage 1; the remaining 60 still hardcode their numbers in combat.rs and are marked "pending migration" on the admin page (they become tunable in later per-class batches). This means any wiki figure copied from a node description can now go stale at any time without a deploy — see the next entry for how a tuned node advertises itself — affects passives|character progression
game/src/adventure_web.rs:passive_override_note (new, pub(crate)) — REQUEST TO THE WIKI SESSION, not a change to your files. A node description is a hardcoded prose string, so it cannot reflect a live value override; a retuned node would otherwise leave every tooltip and /wiki/passives quietly stating the old number. This session added a generated "Tuned: X (default Y)" line beside the untouched prose on the player-facing /passives tree only. `passive_override_note(node) -> Option<String>` is deliberately free-standing and pub(crate) so the wiki graph renderer (adventure_web/wiki.rs:render_wiki_archetype_graph, your file — untouched here) can adopt the identical line with a one-line call at its own node_html closure, next to where it already escapes n.description. Returns None for an untuned node, so adopting it changes nothing until an override actually exists. CSS class `.passive-tuned` already exists in templates/base.html — affects passives|wiki infrastructure

ADOPTED by the wiki session (2026-08-19): adventure_web/wiki.rs:render_wiki_archetype_graph's node_html closure now calls passive_override_note(n) and appends its note to `tip` (data-tip), the identical one-line pattern used on /passives itself. Verified via a disposable, uncommitted test (not left in the suite - it mutates the process-global PASSIVE_OVERRIDES store, which paths.rs's own comment on data_path already documents as a real flakiness hazard for the shared test binary): with no override set, no node in /wiki/passives shows "Tuned:" markup; with an override set on "retaliation", its node's data-tip includes the "Tuned: X (default Y)" line and untouched nodes remain unaffected. /wiki/passives also now documents the two new live features from this log: a "Memories" card (3 slots via STARTING_MEMORY_SLOTS, free save/load bypassing ARCHETYPE_CHANGE_COST/PASSIVE_RESPEC_COST, out-of-combat only) and a short player-facing note that node values can be tuned live, deferring exact numbers to the generated tree's own "Tuned:" markup rather than restating them in prose.

NOTE TO THE WIKI SESSION (2026-08-19) — patch-notes.json coordination. New standing owner instruction: every deploy from this session now adds its own patch-notes.json entry as part of the deploy (player-facing changes get player-facing entries, internal-only releases get a one-line internal marker), so the notes stay an unbroken record going forward. Already added: an "August 19, 2026" entry, heading "Internal: Live-Tunable Passive Values (Admin Tooling)", for this Stage 1 deploy. This session will keep adding one entry per deploy from here on - if you're backfilling the recent gap (everything before this instruction landed: Release 1.1, the Memories feature, Release 2 observability), please don't also add an entry for Stage 1 live-tunables above - already covered.

game/src/adventure/announcements.rs:format_loot_line (chat noise cleanup, a live request) — worn-out-gear and retreat notices no longer post to chat; both used to share this fn's own line with real loot drops. Only the chat announcement stopped - `EncounterResult.broken`/`.retreated` are still populated exactly as before (the /fights dashboard's own direct read of `.broken` is untouched, and the retreat mechanic itself - character.rs's `retreated_since`/`sync_retreat_status` - is a completely separate code path). Own patch-notes.json entry already added ("August 19, 2026", heading "Less Chat Spam: Worn Gear and Retreat Messages Removed") - please don't duplicate it — affects commands

NOTE — everything below is Release 1.2 (branch `feature/release-1.2-golem-adjudication-redistribution-tail`), merged to master and deployed live (2026-08-19). Full per-item detail at docs/elementalist_spec.md's "Release 1.2" section.
game/src/adventure/combat.rs:simulate_battle (thundergolem_reform_base_max_hp re-sync) — real bugfix, item 1: a Thunder Golem's own per-reform max-hp growth was silently undercounting any active party-wide max-hp buff (e.g. Cleric's Blessed Resilience) by a factor of `1/(1+party_max_hp_pct)` on every reform, since the reform base was captured before that buff pass ran and never refreshed. Fixed - a Thunder Golem's reforms now correctly reflect the party's own max-hp buff, same as its initial spawn already did. Increases realized Thunder Golem HP (and therefore absorption capacity) across reforms for any party running a Cleric with Resilience invested — affects passives|combat
game/src/adventure/combat.rs:spawn_golem (crit_chance/crit_multiplier full-value inheritance) — real bugfix, item 2, sibling of the earlier increased_damage/conflagration_dmg_pct fix: golems inherited crit_chance/crit_multiplier at 33% of the owner's own values, which compounds (since both factors scale down together) into a near-zero crit contribution instead of the intended ~33% per-hit ratio - confirmed as the dominant cause of the "~4x shortfall" live finding. Fixed by inheriting both at full value. Meaningful damage increase for any Elementalist running golems with real crit investment — affects passives|combat
game/src/adventure/combat.rs:apply_thunder_redistribution_tick (dead-recipient redirect) — real bugfix, item 3: a redistribution recipient who died from ANY unrelated cause before their own scheduled tick fired had that tick's amount silently vanish (already debited from the golem's own tank-credit, delivered nowhere). Fixed - now redirects to another currently-alive, eligible party member instead of dropping it. Increases realized Thunder Golem redistribution delivery, closing part of the gap below the 50% tunable target — affects passives|combat
game/src/adventure/tunables.rs:LiveTunables::reactive_proc_cap_ms (new) + game/src/adventure_web.rs:/admin/tunables (new "Reactive Procs" section) — item 4, spec-owner ruling: the 1-per-second cap on the shared Rogue's Voidstep/Monk's Counterflow/Druid's Wild Fury counter-attack group (Warrior's Retaliation has no such cap) is now admin-tunable, default 1000ms (matches previous hardcoded behavior exactly, no change unless retuned) — affects passives|commands

NOTE — everything below is Divine Dust (branch `feature/divine-dust`), merged to master and deployed live (2026-08-19). A new per-character currency for making an item Sacred and rerolling its sacred affix. Full spec at docs/divine_dust_spec.md, execution log at DIVINE_DUST_PROGRESS.md (both repo root).
game/src/adventure/character.rs:Character::divine_dust (new) + game/src/adventure/manager.rs:AdventureManager::craft_divine_dust/CraftAction::DivineDust — DIVINE DUST, a new player-facing currency, additive/persisted per character. Three acquisition sources: a fight-drop chance per fighting character per win (LiveTunables::divine_dust_drop_chance, default 0.1, boss and basic wins alike - same eligibility sand's own grant uses), a chance per SACRED item manually disenchanted (LiveTunables::divine_dust_disenchant_chance, default 0.1 - non-sacred disenchants never yield any), and a /craft recipe (LiveTunables::divine_dust_craft_dust_cost/_sand_cost/_craft_output, default 1000 dust + 10 sand -> 1, x1/x10/x50 batchable). All 5 tunables admin-editable at /admin/tunables under a new "Divine Dust" section. Fight drops announce in chat (rare-event framing, same shape as the existing Unique Shard announcement); disenchant and craft-recipe grants are silent — affects crafting|items
game/src/adventure/character.rs:Character::apply_divine_dust (new) + craft.rs:CraftAction::DivineDust — USAGE: applying Divine Dust to an item costs 2 x the item's own tier, on the /craft page next to the other crafting actions. An item not yet Sacred becomes Sacred (see the next entry for a real secondary effect worth knowing about) and gains one random sacred affix from the full affix pool. An already-Sacred item instead rerolls its sacred affix to a different one, current excluded. Respects Krangle's lock (a locked item rejects, same as every other crafting action) — affects crafting|items
game/src/adventure/character.rs:apply_divine_dust (sacralize side effect, worth flagging explicitly) — sacralizing a NOT-YET-PERFECT item via Divine Dust also makes it Perfect (power_roll maxed, every existing affix bumped by the Perfect quality multiplier), not just "gains a sacred affix" on top of whatever quality it already had. This wasn't spelled out in the original feature request as an explicit requirement — it's a judgment call made to keep "Sacred implies Perfect" true everywhere else in this codebase (the auto-disenchant tier ordering, the disenchant-multiplier's Sacred==Perfect+1 equivalence, and this same feature's own disenchant-drop-rate math all assume it). Recorded in docs/divine_dust_spec.md's decisions log — worth a clear callout on whatever wiki page documents the apply/reroll action, since a player reading "gains one random sacred affix" alone would reasonably not expect their item's underlying quality/power to also jump to maximum in the same click — affects crafting|items

game/src/adventure/manager.rs (live bugfix + a live request, 2026-08-19) — two corrections to the Divine Dust entries above, same day as launch: (1) the craft recipe's own `<form>` has no item involved, but `CraftForm::item_a` was a required field, so Axum's own form extractor rejected the recipe's submission with a 422 before the handler ever ran - completely unusable in production. Fixed (`item_a` is now `Option`, validated per-action in the handler instead). (2) Fight-drop chat announcements (`announce_divine_dust_drop`/`format_divine_dust_drop`) are REMOVED per a live chat-noise-reduction request - the entry above stating "Fight drops announce in chat" is now stale, please don't carry that claim into any wiki text. The grant itself (chance, amount, eligibility) is completely unchanged, silent now like the disenchant/craft sources always were — affects crafting|items|commands

NOTE — everything below is the Elementalist rework release (branch `feature/rf-self-damage-mitigation`), a 6-item balance release. Full per-item detail in docs/elementalist_spec.md's Righteous Fire and Golem Master sections (both updated in place, 2026-08-19) — this note is the parser's own prediction-impact flag, not the full spec.
game/src/adventure/tunables.rs:LiveTunables::rf_self_damage_pct_rank{1,2,3} (new) + game/src/adventure/combat.rs:apply_righteous_fire_self_damage (new fn) — items 1-2: Righteous Fire's self-damage tick is no longer a flat 10/20/30%-of-maxHP true-damage hit. It's now its own admin-tunable percentage (still defaulting to 10/20/30%, zero change at defaults), reduced by the caster's own combined damage-reduction sources (base `damage_reduction` + any active temp DR buff, multiplicatively combined - NOT evasion or block, which never apply to self-inflicted damage), with any remainder after DR absorbed by the caster's own active shield before touching HP (a `ShieldAbsorb` roll event is emitted for the absorbed portion). **PARSER IMPACT: any prior check that asserted RF's self-burn tick equals exactly `pct × maxHP` is now wrong for any character with damage_reduction or an active shield - the new formula is `tunable_pct × maxHP × (1 − combined_DR)`, then shield-absorbed before the HP write.** Zero-DR, zero-shield characters still see the exact old numbers — affects passives
game/src/adventure/combat.rs:golem_summon_dmg_penalty (doc-only, item 3) — no code change, a doc/test addition confirming Righteous Fire's own damage (self-burn and enemy-facing alike) has ALWAYS been exempt from the "33% less damage per golem" penalty - RF's damage goes through `apply_true_damage`, which never reads that field (only `resolve_hit`, a normal attack roll, does). Flagging for the parser only because this is now spec-documented and test-locked (`rf_damage_is_unaffected_by_golem_count_while_attacks_still_are`) rather than an unstated implementation detail - no player-facing number changed — affects passives
src/passive_tree.rs:"flamegolem" node (was NotYetImplemented) — item 4, new base effect: all of the Elementalist's own increases to elemental damage (fire/cold/lightning `*_damage_pct` - chaos/divine excluded, not "elemental") are multiplicatively scaled by 1.33x/1.66x/2.0x at rank 1/2/3. Applied once, at player-construction time, so it's already baked into the owner's own stats before Volcanic Ash's existing golem-inheritance line reads them - Flame Golems receive the multiplied scaling too, via that same inheritance, with no separate second application. **PARSER IMPACT: any Elementalist elemental-proc-rate formula must now multiply by this factor for a character with Flame Golem invested; a Flame Golem's own inherited fire-proc rate (via Volcanic Ash) is likewise already-multiplied, not a separate factor to apply again.** — affects passives
src/passive_tree.rs:"watergolem" node (was NotYetImplemented) — item 6, new base effect: Water Golems regenerate 3/6/9% of their OWN max hp per second to every party member (not the golem itself, not any other golem), a real periodic heal (own `SkillCast`+`Heal` events) credited to the OWNING Elementalist. Non-stacking across multiple Water Golems - the strongest is the sole "primary" that actually ticks; every other Water Golem's own clock never fires. **PARSER IMPACT: a new party-wide regen event type exists for any Elementalist party fielding a Water Golem with this base effect invested - not to be confused with Replenishing's existing damage-to-healing conversion, a separate mechanic.** — affects passives
docs/elementalist_spec.md ("Golem full-inheritance doctrine" section, new, item 5) — audited every golem-type field in `spawn_golem` for a passive-effect multiplier still scaled to 33% or omitted entirely; found none beyond what Release 1/1.2 already fixed (increased_damage/conflagration_dmg_pct/crit_chance/crit_multiplier, all at full value) plus item 4's own automatic Volcanic Ash propagation above. Two pre-existing partial-inheritance cases were reviewed and confirmed DELIBERATE, not bugs, and are explicitly excluded from the doctrine: Volcanic Ash's own rank-gated 33/66/100% fire-scaling inheritance (its identity), and Shattering's `splash + rank` target formula (golems never invest splash themselves, so it's always exactly `rank` in practice - pre-existing, documented, no change here). No player-facing formula changed by this item alone - affects passives

NOTE — everything below is a CORRECTIVE release (branch `feature/elementalist-design-intent-fixes`, 2026-08-20) commissioned after a log-analysis audit of the release above. It REVERSES several of that release's own claims and reinterprets items 3/4/5 - the entries above are kept for history but several of their sub-claims are now stale; this note is the parser's own correction, full detail in docs/elementalist_spec.md's Golem Master section (rewritten in place).
game/src/adventure/combat.rs:golem_summon_dmg_penalty (REMOVED, item 1) — the "33% less damage per summoned golem, additive" penalty (the field itself, its `resolve_hit` application, its construction from `golemmaster` rank, every zero-initializer, and the stale doc referencing it) is deleted entirely, not disabled. The Elementalist now deals FULL damage regardless of golem count - no cap, no compensating tunable. **PARSER IMPACT: the ~100x total-power increase at 3 golems (full owner damage + 3 full-strength golems, vs. the old ~1%-owner-plus-golems balance) is accepted as deliberate design, not a bug to flag. Any formula referencing `golem_summon_dmg_penalty` or "33% less per golem" is now wrong - remove it, don't adjust it.** The prior release's own item-3 entry above (RF "exempt from" this penalty) is now moot, since there is nothing left to be exempt from - affects passives|combat
game/src/adventure/combat.rs:tick_righteous_fire/resolve_hit (RF enemy damage re-homed, item 2) — Righteous Fire's enemy-facing damage is no longer a separate periodic true-damage AoE tick. `righteousfire_pct` is now consumed directly inside `resolve_hit` as its own independent multiplicative layer (same shape as Conflagration), applied to the Elementalist's regular unified hit - splash included. `tick_righteous_fire` (still once/second) no longer deals damage of its own; it still exists to apply Relentless Flames/Cauterizing Flames' debuffs to a splash-limited enemy subset and to run Ashes to Ashes' unconditional sweep. RF's SELF-damage (its own 1000ms timer, DR/shield mitigation) is completely unchanged. **PARSER IMPACT: any per-fight "Righteous Fire damage" figure derived from `tick_righteous_fire`'s own event log no longer exists as a separable source - RF's enemy contribution is now folded into the Elementalist's own Attack events (as an `IncreasedDamage` roll source named "Righteous Fire"), the same way Conflagration's contribution already is.** — affects passives|combat
game/src/adventure/combat.rs:spawn_golem (full inheritance broadened, item 3) — supersedes the prior release's own "Golem full-inheritance doctrine": golems now inherit EVERY stat/multiplier/tree-passive effect the owner carries at full value (fire/cold/lightning/chaos/divine damage_pct, splash, lingering_effect_pct, righteousfire_pct, the Focus/Aegis trio, the rest of the Righteous Fire branch), not the previously-enumerated 4-field list. Base stats (`atk`/`max_hp`/`evasion`/`damage_reduction`/`block_chance`) still scale to 33% - that's the ONLY remaining scaled category. Volcanic Ash (Flame Golem) is now confirmed, spec-owner-clarified, to be an ADDITIONAL multiplier ON TOP of that full inheritance, not the inheritance path itself - "inherit 33/66/100% more fire damage" means gain that much MORE than the already-fully-inherited value (e.g. 1000% inherited + 100% Volcanic Ash at rank 3 = 2000% total, not a 1000% fraction). Splash now inherits too, which is what makes Shattering's own `splash + rank` target-count formula meaningful again (previously always reduced to exactly `rank`, deliberately at the time - now genuinely `splash + rank`). ONE deliberate exclusion: `risingphoenix_max_revives` (would make a golem itself an eligible Rising Phoenix caster - a real new mechanic, not a dormant value). **PARSER IMPACT: the prior release's "Volcanic Ash inherits a FRACTION" framing and "golems never get splash" framing are both now wrong - re-derive any golem elemental-damage or Shattering-target-count formula from the corrected spec section.** — affects passives|combat
game/src/adventure/combat.rs:golem_aware_effective_attack_interval (new fn, item 4) — golems now track the OWNING Elementalist's LIVE, accumulating attack-speed buffs (Momentum, Flowing Strikes, Bloodlust, party speed buffs, etc.) every turn, instead of being frozen at a one-time spawn-snapshot that always evaluated to a flat 1.0 speed multiplier (a golem never independently accumulates these stacks itself). `attack_interval_ms` itself is still the golem's own one-time BASE snapshot (Flame Golem's Blazing bonus is unaffected, still baked in once at spawn) - only the LIVE multiplier layer is now sourced from the owner. Real damage-per-second increase for any golem-summoning Elementalist who also invests in a speed-stacking mechanic — affects passives|combat

NOTE — everything below is Elemental Damage on All Slots (branch `feature/elemental-damage-all-slots`), merged to master and deployed live (2026-08-20).
game/src/adventure/affix.rs:affix_def (ColdDamage/FireDamage/LightningDamage/DivineDamage/ChaosDamage's `eligible_slots`) — the 5 elemental-damage affixes are now rollable on EVERY gear slot (Weapon/Helm/Body/Gloves/Boots), not just Weapon/Helm as before. Owner-approved "full widen." Weapon/Helm already had the full 17-affix pool, so this is a no-op for those two; Body/Gloves/Boots go from 12 to 17 eligible affixes each (+42%), diluting every other affix's (including the still-10x-rarer Leech's) realized pick-share on those 3 slots by roughly 31% - no weight values changed, only pool size. Existing already-generated items are unaffected (eligibility only gates NEW rolls - fresh drops, and the 6 affix-adding/rerolling crafting actions); Sacred items' implicit affix (`make_item_sacred`/Divine Dust's own apply/reroll) already ignored slot eligibility entirely, so this widen is also a no-op there. Real, unclamped interaction worth flagging on the Elementalist page specifically: `Character::sum_affix` already reads all 5 equipped slots indiscriminately, so a character can now stack up to 5 independent elemental-affix gear sources instead of 2 - this compounds multiplicatively for Fire/Cold/Lightning through Overshock/Polar Flux/Incinerate's own gear-scaling modifier, and adds flat, uncapped-except-the-global-floor damage for every archetype via `combat_increased_damage`'s damage_type_bonus (Chaos/Divine specifically have no proc-chance clamp on this side at all). The elemental proc-CHANCE side does have a pre-existing hard clamp (`ELEMENTAL_PROC_CHANCE_DIVISOR`) that already sat close to saturating for a few heavy single-element builds under the OLD 2-slot restriction - more characters may now approach that same ceiling, the ceiling itself is unchanged — affects items|passives|combat
game/src/adventure/item.rs:golden_item_baseline::generated_items_match_pre_refactor_baseline (test-only, no player-facing change) — this fixed-seed unit test's expected Body/Gloves/Boots affix updated from Intervene to ColdDamage, since the widened pool changes what the same seed draws. Not a behavior change outside the widen itself, listed here only because the diff might look surprising in isolation — affects (nothing player-facing; internal test coverage only)
NOTE FOR THE PARSER — this merge also regenerated ALL golden-corpus fixtures (every scenario's seeded Body/Gloves/Boots gear generation draws from the widened pool, cascading into different fight logs) - see the corpus-regeneration commit's own message for the full before/after scenario list, deliberate per this feature branch's own commit message ("golden-corpus fixtures are never regenerated from a feature branch - that happens at merge time").

NOTE — everything below is Unified Unique Shards (branch `feature/unified-unique-shards`), merged to master and deployed live (2026-08-20). Merges Celestial Shard and the old Split-Personality-only "Unique Shard" into ONE currency.
game/src/adventure/craft.rs:CraftAction::UniqueShard (redefined) + CraftAction::CelestialShard (RETIRED) — UNIFIED UNIQUE SHARDS: there is now only ONE unique-crafting currency, "Unique Shard" (the name/label/chat-announcement text are unchanged from what "Unique Shard" already meant before this merge - it's the currency identity that widened, not the wording). Consuming a Unique Shard on an item now opens a PICKER (reuses the existing veiled-craft choice-card UI/route, `POST /craft/choose-veil`) offering every `UniqueAffix` effect - currently Celestial Conversion and Split Personality - and the player picks which one to grant, at NO extra cost (no veil surcharge; this was never optional randomness to pay to reveal, just a deterministic menu). Same preconditions as before (item can't already be locked or already unique). CelestialShard itself is retired - nothing drops it any more - but stays a valid legacy value forever for save-data safety; a straggler pre-merge token still works exactly as it always did (grants Celestial Conversion only, no picker) — affects crafting|items
game/src/adventure/manager.rs:maybe_drop_unique_shard (was two independent rolls, now one) + game/src/adventure/tunables.rs:LiveTunables::celestial_shard_drop_chance (default 0.001 → 0.002, field NAME unchanged) — DROP RATE: the recurring fight-drop roll used to be two independent 0.001-chance rolls (one per currency); now it's one 0.002-chance roll for the merged currency, preserving the same total expected income by design (E[2 independent Bernoulli(0.001)] = E[1 Bernoulli(0.002)] = 0.002 either way). The admin page's "Unique Shard Drop Rate" label is unchanged (it already covered both under one number pre-merge). OPERATIONAL NOTE for whoever deploys: if the live `adventure-live-tunables.toml` already has an explicit override of this field (likely, given it's an existing admin-tunable), the code-default change alone won't reach production - the admin value needs a manual bump to actually preserve the "same total income" property on the real server — affects crafting|items|commands
game/src/adventure/manager.rs:maybe_drop_unique_shard (announcement, owner ruling) — the merged currency's drop ALWAYS announces in chat now ("💎 {name} just found a rare Unique Shard!", unchanged text). Before this merge, only the Split-Personality-granting "Unique Shard" half announced; the Celestial-Shard half was silent by design. Owner ruling: "no more celestial shard - only Unique Shards, the old silence rationale retires with the old currency." The two one-time launch-giveaway award paths (top-healer, top-3-performers) are UNCHANGED and stay separate mechanisms, per the owner's explicit ruling not to merge those — affects crafting|commands
game/src/adventure/migrations.rs:migrate_celestial_shard_into_unique_shard (new) — EXISTING PLAYER BALANCES: on first load after this deploys, every character's held CelestialShard token count merges 1:1 onto their UniqueShard count, and the CelestialShard entry is removed. Only the currency migrates - an item that already carries `UniqueAffix::CelestialConversion` (granted before this merge existed) is completely untouched — affects crafting|items

---

NOTE — wiki update pass (2026-08-20), processing the design-intent corrective release, Elemental Damage on All Slots, and Unified Unique Shards above, plus the standing Split Personality FlatStat/OverflowConversion fix (657486b, 2026-08-18, never got its own entry here). Updated: wiki/golems.md (full rewrite of Stats & Inheritance - no more 33%-per-golem penalty, full-value inheritance beyond base stats, live attack-speed tracking, a new "Roles at a Glance" table, real Flame/Water Golem base effects), wiki/classes.md (Righteous Fire's enemy damage folded into normal hits, Golem Master's "no cost" correction, Elemental Focus's widened-affix-slot note, Split Personality's Unique Shard picker + FlatStat fix note), wiki/items.md (elemental affixes now roll on every slot), wiki/crafting.md (Celestial Shard section merged into Unique Shard, currency grid/table/veiling-list updated, anchor id="celestial-shard" kept for redirect-compat), wiki/QUESTIONS_FOR_OWNER.md (open question #2 resolved by the shard merge).

FLAGGING, not documented: the requested "Chain of Light correction (4 bounce targets at max rank, not 5)" is NOT on master as of this pass (verified via `git log`/`git show origin/master:WIKI_IMPACT.md`) - its commit (a81a8ea, "Migrate chainoflight (accepted nerf) and correct the golden-corpus claim") only exists on the still-open `feature/live-tunables-stage2` branch (a separate active worktree), and master's WIKI_IMPACT.md has no Chain of Light entry at all. Not documented in the wiki to avoid stating a number that isn't live yet - please re-request once that branch actually merges to master, or confirm if it already has and this session's fetch was somehow stale.

---

NOTE — everything below is the Golem Inheritance Mechanism release (branch `feature/golem-inheritance-mechanism`), merged to master and deployed live (2026-08-20). This SUPERSEDES the corrective release's own "full inheritance broadened" entry above (item 3) - that release still enumerated an expanded-but-fixed field list; this one inverts the mechanism entirely.
game/src/adventure/combat.rs:spawn_golem (rewritten from allowlist to denylist) — a golem is now built via `summoner.clone()` (every field starts as an exact copy of the owner) with a single hand-maintained exclusion function (`clear_non_inheritable_for_golem`) stripping only what's mechanically wrong to copy: live/transient state (shields, temp buffs, stack counts), tick schedulers, golem-own machinery fields, and 8 fields read by scanning all units rather than indexing the acting unit (Guardian Spirit charges, Frenzy Undying, Soul Stone, Bloodpact, Assassinate, Wildroar, Verdant Burst, Rising Phoenix's max-revives). **PARSER IMPACT: "golems inherit X" is no longer a fact that needs checking per-field against an enumerated list - assume full inheritance of every stat and every tree passive's effect unless it appears in the exclusion list above.** Base stats (atk/max_hp/evasion/damage_reduction/block_chance) still scale to 33%, unchanged. heal_power is the one notable exception with real per-type behavior: zeroed for Thunder/Flame/Basic, additive for Water (`owner.heal_power + replenishing_pct`) - affects passives|combat
game/src/adventure/combat.rs:golem role assignment (Thunder→Melee, Flame→Ranged, Water→Heal, Basic→Melee) — roles are assigned by golem type, NOT inherited from the owner's own role, unchanged from before but now explicitly carved out as its own step rather than incidentally true under the old allowlist - worth confirming the wiki's Roles at a Glance table (added in the prior wiki pass) still states this correctly, since it was written against the OLD mechanism - affects passives
game/src/adventure/character.rs:Character::has_archetype (new) — SPLIT PERSONALITY FIX: every passive/aura gate that checked "is this character archetype X" (43 call sites across combat) now checks "is X either the primary or the Split Personality secondary archetype." Before this, a Split Personality character's SECOND class's passives silently did nothing in most of these 43 gates - only their primary class's passives worked. This is a real, wide-reaching correctness fix, not a new feature - Split Personality has always been sold as "invest in a second class's tree," and most of that tree's own effects weren't actually firing. **PARSER IMPACT: any prior claim that Split Personality "doesn't fully work" or has scattered gaps is now the FIXED state - re-verify before repeating.** — affects passives|commands
game/src/adventure/manager.rs:load_memory (golem_slot_types) — fixed a bug where loading a saved Memory build wiped a character's golem type assignments back to default whenever the Memory itself predated typed golem slots (most saved builds), even if the character's CURRENT live slot assignments were fine - now only overwrites if the Memory actually carries non-empty slot data — affects passives|commands
game/src/adventure/combat.rs:handle_shattering_on_enemy_death (Water Golem's Shattering) — icicle damage is now normal, mitigable damage (subject to the target's own crit/evasion/damage-reduction), not unmitigated true damage as before. Target count formula (splash + rank) is unchanged. Non-stacking across multiple Water Golems, unchanged (only one fires per enemy death) — affects passives|combat
game/src/adventure/combat.rs:thin_events_for_overlay (overlay-only, no gameplay change) — the OBS overlay's live event feed used to flat-cap events per second across every kind uniformly, which incidentally dropped the large majority of Heal and Defeat events under real fight load (healing looked invisible, downed characters kept rendering alive). Heal/Defeat/SkillCast are now always shown regardless of volume; only Attack events are still capped. Presentation-only - the saved fight log and every stat/audit path were never affected by this bug — affects overlay only, no wiki content

---

NOTE — small standalone follow-up (2026-08-20, same day, live request): Water Golem's Shattering modifier is TEMPORARILY DISABLED pending a rework.
game/src/adventure/tunables.rs:LiveTunables::shattering_enabled (new, default true) + game/src/adventure_web.rs:/admin/tunables (new checkbox) — a live kill-switch, currently set to `false` in production (`adventure-live-tunables.toml`). While off, every Shattering proc is a complete no-op - no icicles, no damage, no events. Nothing about the tree node, a player's invested rank, or the tooltip changed; it resumes working the instant an admin re-checks the box (or edits the toml back to `true`) - no redeploy needed either way, since this was built as a live tunable specifically so re-enabling it later needs no code change. **PARSER IMPACT: Shattering is inert on live right now despite being fully implemented in code - don't flag "Shattering doesn't fire" as a bug if reported while this stays off.** — affects passives

---

NOTE — playerVitals regression fix (2026-08-20, release A, standalone, deployed live). Not player-facing beyond the companion app's own health-bar display - no wiki content change expected, flagging for completeness/awareness only.
game/src/adventure/combat.rs:thunder_golem_redirect (ownership-scoped, real bug fix) — this function has had NO ownership scoping since its very first version (predates every release this session touched): it picked the first alive Thunder Golem ANYWHERE in the whole encounter, so in any fight with multiple real players sharing one boss, a player with no Thunder Golem of their own could still have their damage silently redirected onto a COMPLETELY UNRELATED player's golem. Confirmed live via real fight data (~50-player shared encounters): the large majority of players showed 1-2 hpSamples despite hundreds of real hits, while 2-3 Thunder-Golem-owning players' golems absorbed nearly the whole encounter's damage between them. Now scoped to the same owning Elementalist only (via `golem_summoner_id`) - a Thunder Golem only ever tanks for its own summoner, never a stranger. **PARSER IMPACT: this was never caused by the golem-inheritance release (which never touched this function) - it became severe once the earlier corrective release made 3-Thunder-Golem builds free, not because of anything new here.** — affects passives|combat, no wiki page change expected

NOTE — wiki update pass (2026-08-20), processing the Golem Inheritance Mechanism release and the small Shattering follow-up above. Updated: wiki/golems.md (Stats & Inheritance rewritten for the denylist model - "assume full inheritance unless excluded" instead of an enumerated allowlist, added the heal_power per-type special case, added Combat Role - Melee/Ranged/Heal - to the Roles table since it's now explicitly its own assignment step rather than incidental, added the Memory-golem-slot-persistence fix note to Slots & Types, flux-tagged Shattering as temporarily disabled with its return behavior described), wiki/classes.md (Split Personality's note broadened from the earlier FlatStat/OverflowConversion-only fix to the full `has_archetype` fix - 43 gate sites, not just stat pooling).

Also fixed a stale note found while reviewing golems.md, not part of this pass's request but directly contradicted by patch-notes.json's own Aug 19 entry: Thunder Golem's absorbed-damage redistribution was still described as "coming" with "no numbers yet" - it's been live for days (shipped in Release 1) and is fully described in patch-notes.json already. Rewrote to state the actual mechanic (half absorbed redistributes as party damage on death, half forgiven) as current, keeping only the exact-delivery-percentage as the flux-tagged part.

Confirmed, not backfilled: patch-notes.json is already current through the Golem Inheritance Mechanism release, the chat-cleanup entry, and the full Divine Dust era - the deploy session's own standing "write the patch note before the deploy report" convention has kept it complete. No gaps found.

Split Personality's Unique Shard picker and elemental-affix-all-slots content (documented in the prior wiki pass) had no new WIKI_IMPACT entries this pass and were left as-is - still accurate.

Still held per instruction: Chain of Light's 4-target correction (feature/live-tunables-stage2, still unmerged) - not re-verified this pass since the request explicitly said it remains held; will pick up the existing flag once a re-request comes in.

RESOLVED (2026-08-20): the Thunder Golem owner-scoping contradiction flagged above is closed. Owner confirmed party-wide is doctrine - the owner-scoped "fix" was itself a mistake, reverted (see the priority-release note below), and verified live. wiki/golems.md's existing "absorbs all externally-sourced damage the party would otherwise take" wording was correct all along and needed no edit.

---

NOTE — Shattering fixes + re-enable (2026-08-20, release B, deployed live). This is the fix for the exact excursion that got Shattering kill-switched (see the wiki-update-pass note above) - `shattering_enabled` is now back to `true` in production.
game/src/adventure/combat.rs:apply_shattering_icicle_damage (new, replaces the `apply_hit` call the prior wiki-pass note described) — root cause of the reported 110,000x live excursion (and a separate 196x inflation of an unrelated, golem-less Warlock's Curse of Doom): routing icicle damage through the full `apply_hit` pipeline let a fully-inherited golem's ENTIRE crit/increased-damage/elemental-proc stack multiply an already-large "1% of a dead enemy's max HP" base, AND fed `apply_hit`'s own generic Doom-accumulation bookkeeping regardless of which Warlock (if any) actually caused the damage. Icicles now resolve through a dedicated function instead (same shape as Righteous Fire's own self-burn) - the target's damage_reduction still applies (still mitigable, the original ask), but the attacker's own damage-multiplier stack contributes NOTHING, and the event is tagged `Environmental` and never touches Doom's accumulation pool at all. **PARSER IMPACT: both the excursion and the Doom inflation are structurally impossible now, not just numerically smaller - re-verify against real live data once Shattering sees real use again.** — affects passives|combat
game/src/passive_tree.rs:"shattering" node's own magnitude table (repurposed) — the icicle's damage basis (previously a hardcoded 0.01 constant) is now this node's own live-tunable magnitude, default 1% at every rank (matching the original spec, admin-overridable on `/admin/passives` same as any other node value). Target count (1/2/3 extra, `splash + rank`) is UNCHANGED - still read via `passive_node_rank` directly, a completely separate read off the same node. — affects passives
game/src/adventure/tunables.rs:LiveTunables::shattering_enabled (flipped back to `true`) — supersedes the prior wiki-pass note describing this as currently off. Shattering is live again as of this release. — affects passives
Also this release: "Balance: Righteous Fire self-damage at max rank tuned to 60% max HP/sec" (owner-confirmed deliberate live tuning of `rf_self_damage_pct_rank3`, no code change) — affects passives

---

NOTE — deterministic party ordering in simulate_battle (branch `feature/combat-deterministic-ordering`), merged to master and deployed live (2026-08-20).
game/src/adventure/combat.rs:simulate_battle (new `fight_seed` parameter + deterministic party ordering) — INTERNAL DETERMINISM FIX, no player-facing number changed and no fixture moved. The party unit list was built straight off a HashMap whose iteration order Rust randomizes per process, so unit INDEX — which drives "random alive player" targeting and first-mover/same-tick tie-breaks — differed run to run. Units are now sorted by id and then shuffled from the fight's own seed using a separate generator (never the combat rng, so zero draws are consumed from it and every existing golden-corpus fixture still matches byte for byte). Player-visible effect: none to any probability or number. Each player's probability of being targeted is exactly what it was; only WHICH player a given roll maps to changes, and it now varies fairly per fight rather than being fixed by a per-process hash accident. NOT a full replay seed — production still rolls fights from thread_rng, so a live fight is still not reproducible from the fight seed alone. Unblocks multi-character golden-corpus scenarios, which is what will finally give the ally-targeted passive set (covenant, compassion, chainoflight, sharedstrength, unitedpack) any snapshot coverage at all — affects combat

---

NOTE — priority release (2026-08-20), reverting the Thunder Golem owner-scoping fix and completing Shattering's tunable coverage. Deployed live, ahead of the determinism merge above in the actual deploy sequence though this note lands after it in this log.
game/src/adventure/combat.rs:thunder_golem_redirect (REVERTED to party-wide) — the earlier "fix" for playerVitals ticket #33 (owner-scoped absorption, so a Thunder Golem only protected its own summoner) is ITSELF REVERSED. Owner ruling: absorption is PARTY-WIDE per the founding spec ("absorbs all damage the party takes until it dies," confirmed against both this doc's own Golem Master section and docs/elementalist_spec.md's Stage 0 design). **PARSER IMPACT: #33 is CLOSED as NOT A BUG** - flat party hpSamples while any Thunder Golem is alive are the truthful picture of party-wide immunity working, not lost data; expect real timeline movement in bursts when a Thunder Golem dies (its absorbed damage redistributes to the party at that point). The pinned pre-deploy baseline comparison requested to explain any flatness delta could NOT be run - the §13 pinning mechanism only ever pinned the SUMMARY tier (`adventure-fights-summary/`), which never carried `playerVitals`/`hpSamples` at all; only the DETAIL tier does, and it only keeps a ~3-file rolling window, nowhere near far enough back to reach pre-golem-inheritance state. Flagging this as a real gap in the pinning mechanism itself for any future vitals-shaped investigation - affects passives|combat
game/src/adventure/tunables.rs:LiveTunables::shattering_damage_pct_rank1/2/3 (new) — Shattering's icicle damage basis moves OFF the passive node's own magnitude (where Release B briefly put it) onto its own named per-rank LiveTunables, same pattern as `rf_self_damage_pct_rank*`. New general convention (documented on `LiveTunables`'s own doc comment): a node's magnitude carries its PRIMARY value only; additional numeric aspects of the same mechanic get their own named tunables, never crammed into the one magnitude slot. For Shattering the node's own magnitude reverts to representing target count (its primary value, unchanged in practice — still actually read via `passive_node_rank`, the magnitude table is display-only) — affects passives
game/src/adventure_web.rs:/admin/tunables (new Water Golem / Shattering fields) — the 3 new rank-tunables are editable there, with the full formula spelled out in the section's own doc text: targets = splash + the node's own rank value (tune at /admin/passives), damage = damage% (tune here) × dead enemy maxHp × (1 − target DR) — affects passives

---

NOTE — overlay-bandwidth work phase 1 (2026-08-20), deployed live. Internal/infrastructure only - no gameplay or passive content, flagging for completeness.
game/src/adventure_overlay_server.rs:WsParams/handle_socket (opt-in app-layer compression) — a client presenting `?wire=deflate` on `/ws` gets its JSON payloads raw-deflate-compressed and sent as binary frames instead of plain text; every other client (the exact key/value absent) is byte-for-byte unaffected. NOT the WebSocket protocol's own permessage-deflate extension - neither axum 0.7 nor its underlying tungstenite implement that at all (verified directly against both crates' source). The `wire=deflate` key/value is a hard contract with the already-shipped PathOfDust_Desktop 2.6.0 client, not a free choice on this side.
public_adventure_overlay/overlay.html:connect (requests compression by default + `?feed=parent` embed suppression) — the served overlay page now requests `wire=deflate` by default (OBS Browser Sources and direct viewers are both modern-Chromium/DecompressionStream-capable), with a fallback to a plain reconnect if decompression ever fails or the API is unavailable. Also added `?feed=parent`: when present, the page skips opening its own `/ws` socket and listens for the same state/encounter payloads via `window.postMessage` from a parent frame instead - deliberately NOT `?embed=app`, which every already-installed desktop sends today for an unrelated purpose (settings-tray stripping) and must keep working unchanged. — affects overlay only, no wiki content

---

NOTE — #38: damage reduction hard-caps at 95% everywhere (2026-08-20), deployed live. Owner doctrine, generalized from RF/Shattering's own two known gaps: no character, golem, or enemy may ever be immune to any damage source through DAMAGE REDUCTION specifically - no exemptions. Evasion, block, and Paladin's Intervene are separate mechanics with their own existing caps and are explicitly UNCHANGED, as is Thunder Golem absorption/redirect and a golem's own take-no-damage base rule (none of those are damage reduction).
game/src/adventure/tunables.rs:LiveTunables::defensive_stat_hard_cap (new, default 0.95) — the cap is now a live tunable, editable on `/admin/tunables`, replacing the compile-time `DEFENSIVE_STAT_HARD_CAP` constant at every DAMAGE-REDUCTION site (that constant still exists, unconverted, for evasion's own unrelated cap - see below). Baked onto every `CombatSimUnit` at construction time (player, boss, and boss-adds all read the real tunable; golems inherit their summoner's own gear/tree stats as usual but this field itself is construction-time, not inherited) - the same idiom `rf_self_damage_pct` already established, chosen specifically because `resolve_hit`/`apply_hit` are called far too many times per fight (~70 combined call sites) to thread a fresh parameter through instead — affects passives|combat
Full sweep of every `combine_reduction_sources` call site in the codebase, as required - each one categorized:
- combat.rs (`dr_pre_boss`, `dr_combined` inside `resolve_hit`) — CAPPED ALREADY (pre-existing 95% cap, now reads the tunable instead of the constant)
- combat.rs (`dmg_without_curse`, the Curse of Weakness marginal-damage recompute inside `resolve_hit`) — CAPPED BY THIS RELEASE (a real gap: this hypothetical "what if the curse source were absent" recompute was uncapped, which could overstate a curse's own attributed bonus damage past what the real, properly-capped hit actually dealt)
- combat.rs (`reduction`, Lingering Effect DoT tick mitigation) — CAPPED BY THIS RELEASE (a real gap: DoT ticks could be mitigated toward zero with enough stacked DR sources, with no cap at all before this)
- combat.rs (RF self-damage, `apply_righteous_fire_self_damage`) — CAPPED BY THIS RELEASE (the release's own named site #1 - this is the mechanism behind the parser's 152,100/tick figure)
- combat.rs (Shattering icicle damage, `apply_shattering_icicle_damage`) — CAPPED BY THIS RELEASE (the release's own named site #2)
- combat.rs (`pre_boss_evasion` inside `resolve_hit`) — NOT DAMAGE MITIGATION: this is evasion (dodge chance), a separate prevention mechanic explicitly out of scope per the doctrine's own scope guard. Shares the SAME 0.95 ceiling only because the two numbers happen to coincide, via the untouched compile-time `DEFENSIVE_STAT_HARD_CAP` constant - not wired to the new tunable, deliberately
- character.rs (`combat_damage_reduction`) — NOT DAMAGE MITIGATION: a character-level STAT AGGREGATION (gear + tree + Reckless/Death Wish, each individually capped at ±75%) that feeds INTO the real mitigation calculation later - the 95% cap correctly lives at the point of actual consumption (`resolve_hit`), not this intermediate stat getter; capping here too would double-cap
- character.rs (`combat_block_chance`, `combat_evasion`) — NOT DAMAGE MITIGATION: block and evasion stat getters, both explicitly out of scope per the doctrine's own scope guard (separate mechanics, own existing 75% caps)
- character.rs (`combat_intervene`) — NOT DAMAGE MITIGATION: Paladin's Intervene redirect % (who takes a hit, not how much gets reduced), with its own deliberate, unrelated 50% cap by design (documented in place as "this IS the complete per-character combine" for that specific mechanic)
Tests: a character at 200% raw DR takes exactly 152,100 on a 5.07M-hp pool at rank-3-tuned 0.6 self-damage (the parser's own expected figure, matched exactly); the shield-absorb branch confirmed reachable at high DR and proven to absorb the POST-cap remainder, not the pre-cap raw amount; the tunable proven to override real RF self-damage output end-to-end through the actual construction-site wiring.

---

NOTE — wiki update pass (2026-08-20), processing the Thunder Golem revert and Shattering's Release B + priority release above, per direct owner order. Updated: wiki/golems.md (Shattering's flux tag removed - live again - and its mechanics rewritten to the final Environmental-damage design: splash+rank icicle targets, damage = tunable% × dead enemy's own max HP × (1 − target DR), no crit, no multiplier-stack contribution, current default 1% at every rank stated as a tunable default rather than a fixed number). Thunder Golem's own section needed no edit - "absorbs all externally-sourced damage the party would otherwise take" was already correct. No wiki page currently covers companion-app health-timeline/vitals display, so the flat-HP-bars-during-Thunder-Golem-uptime clarification wasn't added anywhere (nothing to mirror it into).

Not wired as a live {{PLACEHOLDER}}: Shattering's damage% (`LiveTunables::shattering_damage_pct_rank1/2/3`) is only reachable via `AdventureManager::live_tunables(&self)`, an instance method - `wiki_placeholder_map()` takes no state/AppState parameter, so exposing it there would mean threading state through the whole markdown-render pipeline, well past an additive change. Stated the current default in prose instead (same convention already used for Righteous Fire's self-damage % and Thunder Golem's redistribution %, both also LiveTunables fields never wired as placeholders). Flagging in case a future pass should thread AppState through the wiki's placeholder map for this whole tunables category - out of scope to decide unilaterally.

Chain of Light: still held per instruction, unchanged.
