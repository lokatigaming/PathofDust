# Wiki Impact Log

Player-facing changes that may require wiki updates. The wiki session consumes
this. Append one line per change; newest at the bottom.

Format: `<file>:<const or fn> — what changed — affects <bosses|crafting|passives|commands>`

---

src/passive_tree.rs:MONK_NODES — Hundred Fists and One Hundred Hands swapped tiers: "One Hundred Hands" is renamed **"Flow like Water"** and is now the Specialization under Flowing Strikes (max rank 4), while Hundred Fists is now a Modifier under Pressure Point (max rank 3). Chakra of Many/Light/Life re-parented from Hundred Fists to Flow like Water. Both nodes' effects and description text are unchanged — only names, tiers, and parents moved — affects passives
src/adventure/combat.rs:onehundredhands_bonus_stacks — clamped to rank 3, so Flow like Water's 4th point only unlocks its Modifiers and does not grant a 4th bonus stack (matches every other Specialization and the node's own "up to 3 at 3/3" text) — affects passives
src/passive_tree.rs:"doom" node — detonation cut from 3/6/9% to 2/4/6% of damage dealt to the cursed target; the node's own description prose was updated to match, so the rendered passive text follows automatically — affects passives
src/adventure/combat.rs:ELEMENTAL_PROC_CHANCE_DIVISOR — 50.0 → 10.0, a 5x increase to how often Fire/Cold/Chaos/Lightning/Divine ailments apply on a landed hit; per-stack strength, stack caps and duration all unchanged — affects passives
src/adventure/combat.rs:apply_late_stage_penalty (new fn) — the late-stage damage penalty vs a real boss now applies to every damage source, not just normal attacks; previously bypassed by Lingering Effect DoT ticks, Warlock's Doom detonation (both on-expiry and on-death variants plus their Apocalypse splash), Mage's Volatile Magic splash, Slayer's Hemorrhage/Wound explosion, and reflected damage. Slayer's Culling Strike execute is deliberately exempt (guaranteed kill, no damage amount to scale) — affects passives
src/adventure/item.rs:Item::reforge_crit_used/recombine_crit_used — Reforge's and Recombine's once-per-lineage crit gates are no longer permanent sticky flags; they're now derived from whether the crit-granted bonus affix is still actually present on the item (`Item::crit_bonus_affixes`, now source-tagged). Removing that affix by ANY means (Annulment, a future crafting method, etc.) re-opens the gate. Old saves migrate via `migrations::migrate_crit_flag_to_affix_tracking` — affects crafting
src/adventure/manager.rs:AdventureManager::new (one-time equipped-item backfill) — every currently-equipped item whose old crit-used flag was true but had no surviving crit-granted affix gets a fresh random bonus affix granted immediately, tagged as a Reforge crit affix — affects crafting
Pure refactor, no behavior change — hoisted these bare literals into named pub(crate) constants so the wiki's constant audit can wire placeholders to them: src/adventure/combat.rs:DRAGON_SLOW_MULT (1.5), src/adventure/combat.rs:FIRE_DEMON_HEAL_MULT (0.5), src/adventure/combat.rs:CTHULHU_DEBUFF_CAP (0.9, Cthulhu's own two read sites only — not the separate Thornedhide/Soul Stone/Bloodpact Triage 0.9 caps), src/adventure/combat.rs:LICH_SUMMON_CADENCE_MS (2000ms), src/adventure/combat.rs:LICH_ADDS_PER_SUMMON (5), src/adventure/combat.rs:LICH_MAX_ADDS (20, hoisted out of simulate_battle's function body to module level), src/adventure/character.rs:RECOMBINE_CRIT_CHANCE (0.05), src/adventure/manager.rs:PANEL_REFORGE_DUST_PER_TIER (30), src/adventure/manager.rs:POLISH_PERFECT_SAND_COST (12), src/adventure/manager.rs:POLISH_SAND_COST_PER_QUALITY_PCT (10.0) — affects bosses|crafting
src/commands.rs:"pinfight" (new mod command) — !pinfight copies the most recent coarse-tier and detail-tier fight-log files into a pinned/ folder that the normal rolling-window pruning never touches, announcing the pinned fight's id in chat; also visible on the /admin/tunables dashboard. Preserves bug-report evidence past the ~5-minute detail-tier window without raising the retention caps for everyone — affects commands
src/passive_tree.rs:"echoingpower" node — tooltip-vs-data fix, not a balance change: description said Spell Echo's re-cast damage reaches 80% at 3/3, but the stored effect (0.15/0.15) has always computed 95% in combat — text corrected to match the real, unchanged data — affects passives

--- WIKI SESSION REQUEST (not a primary-session change — these are bare literals the wiki's "never hardcode a number" rule for the completeness pass can't publish without a name; requesting a hoist to pub(crate) constants, same pattern as the DRAGON_SLOW_MULT batch above) ---
src/adventure/combat.rs:3834,4164,4186 — the 95% hard ceiling on combined Evasion and combined Block+DR (three call sites, same bare `0.95` literal, no shared constant) — needs one named constant, e.g. DEFENSIVE_STAT_HARD_CAP — affects combat
src/adventure/combat.rs (late_stage_damage_penalty formula, doc at :759, computed near :7538) — the `+ 2000` stage-offset in `stage / (stage + 2000)` is a bare literal — needs a named constant, e.g. LATE_STAGE_PENALTY_STAGE_OFFSET — affects combat
src/adventure/character.rs:2066-2068 (`attack_interval_ms`) — the three per-role base attack intervals (Melee 1400, Ranged 900, Heal 1700, all ms) are bare match-arm literals — needs three named constants — affects combat
src/adventure/manager.rs (boss_stats_for's attack-interval-by-party-size formula, `(1100.0 / (1.0 + (party_size - 1.0) * 0.15) / 2.0).round()`) — the 1100/0.15/2.0 magic numbers are unnamed — needs named constants if the wiki should state the exact formula — affects combat
src/adventure/character.rs:2713,2730 (`combat_crit_chance`/`combat_crit_multiplier`) — the `0.05` base crit chance and `2.0` base crit multiplier everyone starts with (before gear/archetype/tree) are bare literals — needs two named constants — affects combat
src/adventure/character.rs:837-839 (`Character::new`) — the starting free-action banks (`free_model_changes: 1`, `free_recombines: 1`, `free_archetype_changes: 2`) are bare literals, separate from the `default_one_u32`/`default_two_u32` serde-migration helpers used elsewhere for the same fields — needs named constants so a fresh character's starting bank matches what old-save migrations grant, by construction rather than by two places agreeing to be updated together — affects character progression
